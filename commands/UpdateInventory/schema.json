{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "UpdateInventoryCommand",
    "type": "object",
    "properties": {
      "sku": {
        "type": "string",        
        "description": "The stock keeping unit ID of the product inventory being updated"
      },
      "quantityChange": {
        "type": "integer",
        "description": "The change in quantity of the product in the inventory. Positive values indicate an increase, while negative values indicate a decrease."
      },      
      "notes": {
        "type": "string",
        "description": "Optional notes regarding this inventory change"
      },
      "timestamp": {
        "type": "string",
        "format": "date-time",
        "description": "The date and time when the inventory update occurred."
      }
    },
    "required": ["sku", "quantityChange", "timestamp"],
    "additionalProperties": false
  }
  