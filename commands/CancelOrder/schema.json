{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CancelOrderCommand",
    "type": "object",
    "properties": {
      "orderId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the order."
      },     
      "notes": {
        "type": "string",
        "description": "Optional notes concering this cancellation."
      }
    },      
    "required": ["orderId"],
    "additionalProperties": false
  }
  