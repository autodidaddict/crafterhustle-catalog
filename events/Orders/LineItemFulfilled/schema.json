{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "LineItemFulfilledEvent",
    "type": "object",
    "properties": {
      "orderId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the order."
      },
      "itemId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the line item fulfilled."
      },
      "notes": {
        "type": "string",
        "description": "Notes regarding the fulfillment of this item"
      },
      "timestamp": {
        "type": "string",
        "format": "date-time",
        "description": "The date and time when the order was amended, in ISO 8601 format."
      }
    },
    "required": ["orderId", "itemId", "timestamp"],
    "additionalProperties": false
  }
  