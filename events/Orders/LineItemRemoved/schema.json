{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "LineItemRemovedEvent",
    "type": "object",
    "properties": {
      "orderId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the order."
      },
      "itemId": {
        "type": "string",
        "format": "uuid",
        "description": "The unique identifier of the line item removed."
      },
      "notes": {
        "type": "string",
        "description": "Optional notes describing the nature of the item removal"
      },
      "timestamp": {
        "type": "string",
        "format": "date-time",
        "description": "The date and time when the item was removed, in ISO 8601 format."
      }
    },
    "required": ["orderId", "itemId", "timestamp"],
    "additionalProperties": false
  }
  